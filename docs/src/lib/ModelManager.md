# ModelManager

The top-level module and the entry point for the API reference.

ModelManager re-exports its public API from the source files under `src/`. The
individual symbols are documented on the grouped pages listed under **Index** in the
sidebar:

- **Core** — [Globals & initialization](@ref), [User API](@ref), [Utilities](@ref), [Trial hierarchy](@ref)
- **Project & inputs** — [Project configuration & locations](@ref), [Variations & designs](@ref), [XML utilities](@ref)
- **Running simulations** — [Runner](@ref), [AbstractSimulator interface](@ref), [Recorder](@ref), [HPC & SLURM](@ref)
- **Analysis & calibration** — [Sensitivity analysis (GSA)](@ref), [Calibration](@ref calibration_lib)
- **Management & maintenance** — [Database](@ref), [Deletion](@ref), [Schema migrations](@ref), [Package version](@ref)

To find a specific symbol, use the [Alphabetical index](@ref) or the search box.
