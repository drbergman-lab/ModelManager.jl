# ModelManager

The top-level module and the entry point for the API reference.

ModelManager re-exports its public API from the source files under `src/`. The
individual symbols are documented on the grouped pages listed under **Index** in the
sidebar:

- **Core** — [Globals & initialization](@ref), [User API](@ref), [Utilities](@ref), [Trial hierarchy](@ref)
- **Project & inputs** — [Project configuration & locations](@ref project_configuration_lib), [Variations & designs](@ref variations_lib), [XML utilities](@ref)
- **Running simulations** — [Runner](@ref), [AbstractSimulator interface](@ref), [Recorder](@ref), [HPC & SLURM](@ref hpc_lib)
- **Analysis & calibration** — [Sensitivity analysis (GSA)](@ref sensitivity_lib), [Calibration](@ref calibration_lib)
- **Management & maintenance** — [Database](@ref database_lib), [Tags](@ref), [Deletion](@ref), [Schema migrations](@ref), [Package version](@ref)

To find a specific symbol, use the [Alphabetical index](@ref) or the search box.
