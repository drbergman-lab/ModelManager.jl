```@meta
CollapsedDocStrings = true
```

# Deletion

Deleting simulations and resetting the project database.

`deleteCalibration` removes a calibration run and is documented on the
[Calibration](@ref calibration_lib) page, alongside the rest of that subsystem.

```@autodocs
Modules = [ModelManager]
Pages = ["deletion.jl"]
Private = false
```
